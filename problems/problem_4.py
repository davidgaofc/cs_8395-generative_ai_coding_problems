#return the solution only with no talking keeping in mind that it will be directly inputted into a python file
import re
from collections import Counter


def clean_text(text):
    """
    Cleans the input text by removing non-alphanumeric characters and converting to lowercase.

    Args:
        text (str): The input text to be cleaned.

    Returns:
        str: The cleaned text.
    """
    # TODO: Implement this function
    pass


def count_word_frequency(text):
    """
    Counts the frequency of each word in the input text.

    Args:
        text (str): The input text.

    Returns:
        dict: A dictionary containing word frequencies.
    """
    # TODO: Implement this function
    pass


def most_common_words(text, n):
    """
    Returns the most common words in the input text along with their frequencies.

    Args:
        text (str): The input text.
        n (int): The number of most common words to retrieve.

    Returns:
        list: A list of tuples containing the most common words and their frequencies.
    """
    # TODO: Implement this function
    pass