#return the solution only with no talking keeping in mind that it will be directly inputted into a python file
import os

def file_search(root_dir, file_extension):
    """
    Search for files with a given extension within a directory and its subdirectories.

    Args:
        root_dir (str): Root directory to start the search.
        file_extension (str): Extension of the files to search for.

    Returns:
        list: List of file paths matching the given extension.
    """
    # TODO: Implement file search
    pass