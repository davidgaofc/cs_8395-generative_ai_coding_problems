#return the solution only with no talking keeping in mind that it will be directly inputted into a python file
import numpy as np

class MatrixOperations:
    def __init__(self):
        """
        Initializes a matrix operations manager.

        This class provides methods to perform various matrix operations using NumPy.
        You are required to implement matrix addition, multiplication, and transpose
        using the provided functions. Make sure to use the NumPy library for all matrix operations.
        """

    def matrix_addition(self, matrix1, matrix2):
        """
        Adds two matrices using NumPy.

        Args:
            matrix1 (np.ndarray): First matrix.
            matrix2 (np.ndarray): Second matrix.

        Returns:
            np.ndarray: Resultant matrix after addition.
        """
        pass

    def matrix_multiplication(self, matrix1, matrix2):
        """
        Multiplies two matrices using NumPy.

        Args:
            matrix1 (np.ndarray): First matrix.
            matrix2 (np.ndarray): Second matrix.

        Returns:
            np.ndarray: Resultant matrix after multiplication.
        """
        pass

    def matrix_transpose(self, matrix):
        """
        Transposes a matrix using NumPy.

        Args:
            matrix (np.ndarray): Matrix to be transposed.

        Returns:
            np.ndarray: Transposed matrix.
        """
        pass